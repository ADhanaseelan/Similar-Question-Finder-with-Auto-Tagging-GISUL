services:
  # ----------------------------------------
  # Unified Python FastAPI + Next.js App
  # ----------------------------------------
  - type: web
    name: learnconnect-unified
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
